<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.campus.device.dao.LogMapper">

    <resultMap id="LogResultMap" type="com.campus.device.model.entity.OperationLog">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="username" column="username"/>
        <result property="operation" column="operation"/>
        <result property="method" column="method"/>
        <result property="requestUrl" column="request_url"/>
        <result property="requestParam" column="request_param"/>
        <result property="result" column="result"/>
        <result property="ip" column="ip"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <insert id="insert" parameterType="com.campus.device.model.entity.OperationLog"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO operation_log (user_id, username, operation, method, request_url,
                                   request_param, result, ip, created_at)
        VALUES (#{userId}, #{username}, #{operation}, #{method}, #{requestUrl},
                #{requestParam}, #{result}, #{ip}, #{createdAt})
    </insert>

    <select id="selectAll" resultMap="LogResultMap">
        SELECT id, user_id, username, operation, method, request_url,
               request_param, result, ip, created_at
        FROM operation_log
        <where>
            <if test="username != null and username != ''">
                AND username LIKE CONCAT('%', #{username}, '%')
            </if>
            <if test="operation != null and operation != ''">
                AND operation LIKE CONCAT('%', #{operation}, '%')
            </if>
        </where>
        ORDER BY created_at DESC
    </select>

</mapper>
