<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.campus.device.dao.LoginRecordMapper">

    <select id="selectByUserId" resultType="com.campus.device.model.entity.LoginRecord">
        SELECT * FROM login_record WHERE user_id = #{userId} ORDER BY login_time DESC LIMIT #{limit}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO login_record (user_id, username, login_time, login_ip, login_device, status, message)
        VALUES (#{userId}, #{username}, #{loginTime}, #{loginIp}, #{loginDevice}, #{status}, #{message})
    </insert>

</mapper>
