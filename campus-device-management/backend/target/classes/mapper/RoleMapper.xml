<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.campus.device.dao.RoleMapper">

    <resultMap id="RoleResultMap" type="com.campus.device.model.entity.Role">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="description" column="description"/>
    </resultMap>

    <select id="selectAll" resultMap="RoleResultMap">
        SELECT id, name, description
        FROM role
        ORDER BY id ASC
    </select>

    <select id="selectById" resultMap="RoleResultMap">
        SELECT id, name, description
        FROM role
        WHERE id = #{id}
    </select>

    <select id="selectByName" resultMap="RoleResultMap">
        SELECT id, name, description
        FROM role
        WHERE name = #{name}
        LIMIT 1
    </select>

    <insert id="insert" parameterType="com.campus.device.model.entity.Role"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO role (name, description)
        VALUES (#{name}, #{description})
    </insert>

    <update id="update" parameterType="com.campus.device.model.entity.Role">
        UPDATE role
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="description != null">description = #{description}</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM role WHERE id = #{id}
    </delete>

</mapper>
