<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.campus.device.dao.UserProfileMapper">

    <select id="selectByUserId" resultType="com.campus.device.model.entity.UserProfile">
        SELECT * FROM user_profile WHERE user_id = #{userId}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user_profile (user_id, avatar_url, department, position, last_login_time, last_login_ip)
        VALUES (#{userId}, #{avatarUrl}, #{department}, #{position}, #{lastLoginTime}, #{lastLoginIp})
    </insert>

    <update id="update">
        UPDATE user_profile
        <set>
            <if test="avatarUrl != null">avatar_url = #{avatarUrl},</if>
            <if test="department != null">department = #{department},</if>
            <if test="position != null">position = #{position},</if>
            <if test="lastLoginTime != null">last_login_time = #{lastLoginTime},</if>
            <if test="lastLoginIp != null">last_login_ip = #{lastLoginIp},</if>
        </set>
        WHERE user_id = #{userId}
    </update>

</mapper>
